{
  "name": "wfs",
  "version": "1.0.0",
  "packageManager": "pnpm@8.8.0",
  "scripts": {
    "dev": "turbo dev",
    "lint": "turbo run lint",
    "clean": "turbo clean && rm -rf .turbo && rm -rf node_modules"
  },
  "workspaces": [
    "apps/*"
  ],
  "license": "ISC",
  "devDependencies": {
    "eslint": "^8.57.0",
    "prettier": "^3.2.5",
    "turbo": "^2.0.0"
  }
}
